[program]

void main(int x, bool b) {
    int y;

    observe;

    if (b) {
        @ y = 1;

        while (y > 0) {
            y = y - 1;
            x = 2 * x;
        }
    } else {
        y = 1;

        while (y > 0) {
            y = y - 1;
            x = 4 * x;
        }
    }

    observe;
}


[formula]
forall pi. forall pii.  {x_pi = x_pii} -> X {x_pi = x_pii}

[depth]
20

[outputs]
